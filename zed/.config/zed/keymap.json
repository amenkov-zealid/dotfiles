// Custom keybindings on top of base_keymap. See https://zed.dev/docs/key-bindings
[
  {
    "context": "Workspace",
    "bindings": {
      // Rerun the last task (e.g. the last pytest run), like PyCharm's re-run.
      "cmd-alt-r": "task::Rerun",
    },
  },
]
